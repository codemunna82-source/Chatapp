import React from 'react';
import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export function ChatsListScreen() {
  return (
    <PlaceholderScreen
      title="Chats"
      note="The FlashList chat list — pinned conversations, unread badges, search — arrives in Phase 7."
    />
  );
}
